{
  "name": "@cf-agents/worker-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker API for CF Agents",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "seed": "node scripts/seed.js"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.0.0",
    "@cloudflare/workers-types": "^4.0.0",
    "hono": "^3.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
